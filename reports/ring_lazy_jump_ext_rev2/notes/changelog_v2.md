# CHANGELOG_v2

- Added v2 report `ring_lazy_jump_ext_rev2_cn.tex` with front-loaded abstract, symbol quick reference, reproduction chain, and data provenance table.
- Replaced window bins with three full-width stacked bars (Fig.14 style) in the same axis as $f(t)$.
- Moved sensitivity analysis into the main text and expanded plots (threshold grid, window shift/width deltas, MC CI bars).
- Added sensitivity analyses: threshold scan, window boundary shift, and MC uncertainty with updated visuals.
- Added Makefile targets for fig2, sensitivity, pdf, check, and clean.
- Added schema + example inputs for fig2 JSON and ft-csv.
