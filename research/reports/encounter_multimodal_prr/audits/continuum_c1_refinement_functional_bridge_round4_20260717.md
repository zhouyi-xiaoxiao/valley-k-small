# Continuum C1 refinement and functional bridge: round 4

Date: 2026-07-17

Status: **LOCAL HASH-SPECIFIC PROOF-CANDIDATE PASS / CONTRACT AND NEUTRAL FIXTURE PASS / P0=0 / P1=0 / P2=0 IN THE AUDITED SCOPES / COMPLETE C1 HOLD / C2-C3 HOLD / PRR RELEASE HOLD**

## Decision

This round closes a coherent local successor layer, but it does not close
complete C1.  The exact bytes below now provide:

1. a result-blind mathematical proof candidate for the relative OU,
   vertex-dual OU, periodic free-diffusion, finite tensor, physical-volume
   killing, and positive-time functional-calculus steps;
2. a machine-readable ideal refinement/rate contract tied to three explicit
   non-result sources;
3. a neutral finite-grid numerical fixture for the new endpoint, periodic,
   and tensor identities; and
4. a conditional reader-facing proposition in the theorem-first Supplement.

The abstract proposition says what follows **if** the reconstructed resolvent
premise is established.  It does not assert that the model-specific relative,
periodic, vertex-dual, tensor, killing, production, or box-exhaustion premises
have already been proved.  All complete-C1, production-bridge, quantitative-
C2, box-C3, topology-transfer, positive-budget, and release flags remain
false.

These are local adversarial development audits tied to exact hashes.  They are
not an external mathematical referee report, journal acceptance, or a PRR
submission decision.

## Exact final bytes

Mathematical successor:

- proof-candidate note SHA-256:
  `17b987d5090618e5346f81217afed7e57daccf878d4b93b8402724b3e002a562`
  (706 lines);
- predecessor fixed-1D note SHA-256:
  `11a3015cbaa38cd58d763052adf3858ad055f121cc83669aaeeb462252d35e79`.

Ideal refinement/rate contract:

- candidate artifact:
  `93b13d8c6864c54896ff2d71d143856554d8e2de94acd8ba4f43cc3a2534987b`;
- builder:
  `a5ca3bae9387c6f0386371f78bfa7407a55810a8143fe42684c912fa7373e877`;
- standalone verifier:
  `5f8517d66f416f981d6f6dff081b8dd8a6ec5e82b6bf5082f35e7628db7a022a`;
- theorem-note pin:
  `2c26789a0290eb61b5e1dc5bf7f445a111c8b86e0d04dd082b135bf49b2b4792`;
- static tests:
  `bed7c42258cfc440e6f597ff1e9104260454f2d9fd45eaa91c1d64b14431395a`;
- currentness/open-set tests:
  `488b7ff447c221b159651c0b983102699635639ea0a56d7ea93c4d891f666cc2`;
- adversarial tests:
  `0568abd3f4aa55e1c4702d35405d56ae217b39275b600526afa73cd76dff2e82`.

The three frozen contract sources are:

- configuration-family source:
  `063913c7fbc2b706ba85a0e3f06005bad23a2292749817294cbf41f5cdce4084`;
- C0 mathematical source:
  `522bd667e5f6fd6a4d12f270f0c2f4b9e86be9b207d471961d4f67db972df559`;
- theorem note: the `17b987...a562` bytes listed above.

Neutral free-axis/tensor fixture:

- generator:
  `f640496395526da42055f023f5c724903acba8a0339960c137538770f2aa2b81`;
- artifact:
  `363814071e06a369b234034f41d347933c893ae0c6efeb543db91e099e88c14c`;
- static/formula tests:
  `0d0e7b4ee6740b433a99fda19a4b9463d97bb300294f3c41813a61a4486d6e6f`;
- mutation tests:
  `366c42e5547b004bd6e2f9c3b600cf0164445b996f39a55a8c2e5c3d44fb56bc`.

Reader-facing working set:

- main TeX:
  `264ab814b067e8e1195455f4d0cbcf8ff21477ded5a0581fc2b01eddd204f72a`;
- Supplement TeX:
  `5c02b4f242d5d644854e8c597e2c273104cd3489dd92231586c998b01ef8a75a`;
- fail-closed compiler:
  `15098db6e731e23a31967077b79ace723849b5e8383169bb497fa57f9b92725e`;
- compile manifest:
  `03a9be39a44e16db66f65ff41ce48fcf5ff640702e9f35c16d072d126d4c8e81`;
- seven-page main PDF:
  `78e2e5169f0397073e4edc3deaad27bf4563f856999eea8468d0e698b51f306a`;
- twenty-four-page Supplement PDF:
  `a3716ded14c480188c3504ad86e7318129aa32ada57ada2caf6b7767d26c9cf7`.

The living Round-167 successor-set regression is:
`b1c0f8f5a7fdcbaecfaeb225a7d6b4412e5b584143bdbe1242569a911a021aa8`.
It adds the current Supplement source and PDF to the declared living
successors while preserving every historical frozen hash in its immutable
audit.

## Mathematical attack and repair chronology

The mathematical note was reviewed in narrow layers before a final same-byte
review:

1. The fixed-1D clarification pass returned `P0=0`, `P1=0`, `P2=2`.  The
   final bytes add recovery for functions outside the finite form domain and a
   complete variational minimizer/norm argument for reconstructed resolvents.
2. The varying-space functional bridge pass returned `P0=0`, `P1=0`,
   `P2=3`.  The repaired proof keeps the compression defect
   `P_hJ_h-I`, proves the resolvent-power telescoping bound, uses zero-constant
   resolvent polynomials dense in `C_0([0,infinity))`, and separates pointwise
   convergence from a finite positive-time net.
3. The axis-extension pass returned `P0=0`, `P1=0`, `P2=4`.  The repairs make
   the vertex endpoint order only `O(h)`, keep the interior and edge estimates
   distinct, represent the unique periodic wrap, and state the tensor/global-
   gauge conventions explicitly.
4. The fixed/vertex/killing line review returned `P0=0`, `P1=0`, `P2=1`.
   The final killing identity uses physical-volume cell averages and the exact
   reconstructed multiplier `K_h^{pc}=V_h/rho`.
5. Two final editorial/mathematical P2 points were repaired.  The final
   706-line same-byte review then returned `P0=0`, `P1=0`, `P2=0` within the
   stated candidate scope.

The main conclusion of the functional bridge is deliberately positive-time
only.  For `r=0,1,2` and every `0<tau<=T<infinity`, it transfers the
reconstructed resolvent limit to uniform convergence of
`L_h^r exp(-tL_h)` and to moving observable pairings.  It makes no uniform
`t=0` claim, no computable spatial/evaluator rate, and no continuum root-
margin claim.

## Contract review and closure of all three earlier P2 findings

The first independent contract audit found no P0 or P1 and three P2 items:

1. fixed nondegenerate intervals and positive density normalizers were
   implicit;
2. several semantic fields were protected by the whole-artifact hash but not
   classified by field-specific verifier checks; and
3. the source-open test compared basenames rather than full relative paths and
   did not check exact open counts.

The final bytes repair all three without promoting the claim:

- `ell_z<r_z`, `ell_r<r_r`, `C_z>0`, and `C_r>0` are explicit;
- the verifier checks `D`, `W`, `gamma`, the complete three-axis substitution,
  all four axis/cell formula families, tensor edge conductance, and the full
  functional-calculus boundary field by field; and
- builder and verifier receipts expose complete report-relative source paths
  and count one read-only open per path.  Currentness tests reconstruct those
  paths through descriptor-relative `dir_fd` walks and compare builder and
  verifier independently.  The hook records every report-relative `.json` or
  `.md` open before comparing the complete `Counter`; a sentinel deliberately
  opens the candidate JSON and confirms that the extra path is detected.

The repaired contract suite contains 10 static, 8 currentness, and 61
adversarial tests: **79/79 PASS**.  Both `build --check` and the standalone
verifier report the candidate hash above, twelve finite anchors, each source
opened exactly once, no result/control or positive-budget scientific read,
and `complete_c1=false`, `production_bridge_proved=false`, and
`release_eligible=false`.

## Neutral numerical findings

The independently re-audited fixture contains no positive-budget or production
claim.  Its 11 static and 10 mutation tests pass: **21/21 PASS**.  An
independent standard-library formula oracle and an in-memory regeneration both
match the exact 30,266-byte artifact.

The recorded checks include:

- midpoint diffusivity `d=D/2` and relative diffusivity `d=2D`;
- approximately second-order cell-centred `rho`, edge, and smooth-form errors;
- vertex endpoint `rho` order `1.01535`, versus interior `1.97428`, edge
  `1.98855`, and smooth-form `1.99977`;
- exact endpoint half volumes and the factor-two endpoint outgoing rate;
- periodic base/half-shift rows with respectively zero and one wrapped cell,
  exact mass/rate/conductance identities, and Fourier order `1.99921786`;
- exact separable tensor sentinels `norm=45/2` and `energy=55117/420`; and
- a virtual `256^3=16,777,216`-cell case represented by 768 axis values, with
  no dense three-dimensional allocation.

All eight fixture promotion flags remain false.  Finite observed orders and
identities are diagnostics, not a proof of an `h -> 0` theorem.

## Manuscript integration and rendered-PDF audit

The seven-page main text now states only that the Supplement proves the
abstract implication after a reconstructed resolvent limit has been
established.  Proposition S3.3 in the twenty-four-page Supplement contains the
maps, compression-power defect, `C_0` functional calculus, positive-time net,
and moving-pairing proof, followed by an explicit list of model-specific
premises that are not asserted.

A local read-only final review of these exact bytes returned
`P0=0`, `P1=0`, `P2=0`.  The fail-closed compiler reproduced the main and
Supplement hashes above from byte-identical isolated builds.  The manifest
records `PASS_INTERNAL_THEOREM_FIRST_WORKING_SET`, all fonts embedded, zero
Type3 fonts, no NUL/replacement characters, no overfull boxes, no undefined
references or citations, Ghostscript parse success, and
`release_eligible=false`.

As a separate visual check, the PDFs were rendered and inspected at main page
3 and Supplement pages 5--7.  The proposition and its page transitions have
no clipping, overlap, or defective break.  The current analytical working set
is therefore **7 main + 24 Supplement = 31 physical pages**.  This page count
is a normal main-plus-Supplement architecture, not a five-page complete paper.

## Executed checks

- contract build `--check`: PASS;
- standalone contract verifier: PASS;
- contract tests: 79/79 PASS;
- neutral fixture tests: 21/21 PASS;
- deterministic/in-memory fixture reproduction: PASS;
- current C0-v2 builder and verifier: PASS with complete C0 false;
- current C0-v3 builder and verifier: PASS with complete C0 false;
- explicit 17-module strict-continuum, manuscript, and historical-freeze
  regression: **236/236 PASS**;
- Ruff and Python syntax compilation on the new contract/fixture modules:
  PASS;
- fail-closed manuscript compiler: PASS, reproducing the 7+24 PDF hashes; and
- rendered PDF structure, fonts, text hygiene, and sampled visual layout:
  PASS;
- research-summary refresh: PASS; and
- report registry, archive manifests, documentation paths, and scientific
  guardrail wiring: PASS.

No network access was used in this round.  The contract and fixture audits
used explicit file lists and did not open result, control, scratch, or
positive-budget scientific payloads.

## Retained nonclaims and next route

This round does **not** establish:

- a separately accepted exact-control-value source or the production
  raw-to-gauged application enclosure;
- generalized Mosco tensorization for the full free box, despite the direct
  reconstructed strong-resolvent candidate;
- exact control-specific physical-volume killing averages in the production
  evaluator;
- a quantitative cut-cell/spatial/evaluator error or C2 rate;
- `r=1,2` box exhaustion, C3, or a continuum root-margin certificate;
- F0/F1/F3 positive-budget science; or
- complete C0, complete C1, release, submission, or journal acceptance.

The next strict-continuum sequence is:

1. close a self-contained varying-space strong-resolvent-to-Mosco equivalence
   or a direct finite-tensor Mosco proof;
2. bind exact physical-volume killing averages and the global gauge to the
   production evaluator without reading result-bearing payloads;
3. select and prove the quantitative C2 route, keeping cut-cell,
   discretization, and evaluator widths separate;
4. prove the `r=1,2` box-exhaustion/C3 step; and
5. only then attempt componentwise root transfer and same-budget science.
