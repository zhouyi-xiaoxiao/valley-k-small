# Round 163: continuum-program v2 repair and independent re-audit

Date: 2026-07-14

Decision: **DESIGN NOTE P0 = 0 / P1 = 0 / P2 = 0 AFTER REPAIR /
OPERATOR TEMPLATE ONLY / C0--C7 SCIENTIFIC CHAIN OPEN / HOLD CONTINUUM CLAIM /
NO POSITIVE-B / NO F1 / HOLD PRR**

This round independently attacked the strict-continuum research program as a
proof contract, repaired every finding from the first attack, and repeated the
attack on the final exact bytes.  It did not prove convergence or run a
scientific calculation.  No network, prospective control value, positive-
budget row, F1 artifact, production state space, or manuscript source was used.

## Exact final bytes

| Object | SHA-256 |
| --- | --- |
| `notes/continuum_research_program_v2.md` | `7e075e83b23bd711c233f21cdb4e02bb60093ce7827abff70ad26bcb33abe286` |
| `code/test_continuum_research_program_v2_scope.py` | `00145c8991975f558cf2b289b7937a59c27583c1a02172a1ee5461c45181fdcf` |
| `notes/research_contract.md` | `011dd25b3a57339a8325702ae4337aaf57c18292881c36f7391e11e1a270f136` |
| `notes/continuum_next_stage_path.md` | `537094d58158aff3fe63a7e589cd979380f7c50ead29818c0f402c23a7410a66` |
| `code/test_general_dimension_scope_consistency.py` | `a96b0caa4003037bb886817653fb999f142f0f68b72f6b81458146a2f0f4d004` |

The living contract and next-stage path were refreshed only to record the
accepted Round-161/162 method scope, preserve F0 as open, and replace the old
custom interprocess route with ordinary canonical local files, exact hashes,
a result-blind clean run, and an independently implemented numerical replay.
This is a reproducibility route, not a new physical claim.

## First independent attack

The initial v2 program, SHA-256
`a09e5bd09003ddcf0bf34f2579840ed8fd4d9b0806b3242d3a2477f3df331927`,
received **P0 = 0 / P1 = 4 / P2 = 4**.

The P1 findings were:

1. the form and Feynman--Kac probability language did not explicitly bind
   positive `D,gamma,W`, simplex controls, or a nonnegative unit-mass initial
   law;
2. the symmetric OU exit formula lacked the strict `R > R0` premise, and the
   asymmetric recipe could square a nonpositive margin;
3. the varying-Hilbert comparison lacked a fixed reversible-mass gauge,
   bidirectional `J_h/P_h` consistency, norm/adjoint control, converging data,
   and the moving observable pairing; and
4. pointwise strong-resolvent convergence had not been upgraded to a uniform
   positive-time statement for `lambda^r exp(-t lambda)`, `r=0,1,2`.

The P2 findings were missing physical units, an explicit algebraic form core,
an incomplete root-displacement denominator/residual rule, ambiguous shared
tube/gap endpoints, and an overstrong statement that a concrete continuum
target was already fixed.

## Repairs verified on the final hash

The final note now requires

```text
D > 0, gamma > 0, W > 0
w_j >= 0 and sum_j w_j = 1
q0 in X_pi, q0 >= 0, and integral q0 = 1
```

and separately states how a declared subprobability mass factor would enter.
It records the physical units of `F`, its first two derivatives, every error
component, root margins, and time displacement.

The closed OU--Doi form now has an explicit
`C_c^infinity(R^2) tensor C^infinity(T_W)` core.  The discrete reversible
measure uses the gauge `sum_i pi_h,i = integral_Omega_L pi`, with the scale
enclosed and hashed as C0 data.  Both `J_h P_h` and `P_h J_h` consistency,
uniform norm bounds, asymptotic isometry/adjoint properties, initial-data
convergence, observable convergence, and moving pairings are explicit gates.

For `f_r,t(lambda)=lambda^r exp(-t lambda)`, the note now requires uniform
sup-norm equicontinuity on `[tau,T]`, a finite-time-net argument, and the
moving-data pairing before it permits qualitative uniform observable
convergence.  It still does not assign a computable rate to that conclusion.

The symmetric OU bound is conditional on a strictly positive distance from
the initial support to the face.  The asymmetric form uses separate left and
right positive margins; if the mean is outside or either margin is
nonpositive, the bound is set to one.  The direct exit argument remains only
an order-zero observable input, never a first- or second-derivative bound.

Root tubes and complement gaps now have an explicit shared-endpoint and
half-open serialization convention.  The conditional displacement bound uses

```text
(epsilon_1 + finite_volume_root_residual) / (kappa_* - epsilon_2)
```

with single-booking of evaluator uncertainty.  The main signature-transfer
condition itself is unchanged.

Finally, the status was narrowed from a fixed continuum target to a fixed
operator template.  A concrete hash-bound C0 model remains open.

## Independent re-audit verdict

The final SHA received **P0 = 0 / P1 = 0 / P2 = 0 within design-note scope**.
The independent reader confirmed that all eight initial findings were closed
without introducing a positive-budget value, prospective weight, F1 output,
or continuum promotion.  Eight frozen scope tests and the living
claim-surface tests passed.

This verdict means only that the research program now states the right objects,
assumptions, dependencies, units, and fail-closed implications.  It is not a
proof of any one of those open dependencies.

## Exact remaining scientific gates

```text
concrete hash-bound C0 model contract       = OPEN
fixed-box Mosco / strong-resolvent theorem = OPEN C1
computable spatial errors r=0,1,2          = OPEN C2
OU order-zero truncation input              = DERIVED INPUT ONLY
box derivative errors r=1,2                = OPEN C3
verified production evaluator              = separate open F0 dependency
error composition and actual root transfer = NOT EXECUTED C5/C6
independent clean continuum audit           = OPEN C7
broad-family continuum signature            = HOLD
positive-B / all 36 F1 rows                 = NOT AUTHORIZED / NOT RUN
```

The strongest admissible present phrase remains “continuum-consistent
numerical evidence” with exact finite mesh/alignment/box scope.  “Continuum
verified,” “proved convergence,” and an exact broad-family continuum mode
assignment remain prohibited.
