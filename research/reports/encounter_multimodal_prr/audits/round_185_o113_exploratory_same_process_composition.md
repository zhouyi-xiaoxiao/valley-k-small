# Round 185: O113 exploratory same-process composition

Date: 2026-07-19

Status: **PASS AT BOUNDED EXPLORATORY COMPONENT SCOPE /
MATHEMATICS P0=0/P1=0/P2=0 / CODE P0=0/P1=0/P2=1 /
DISCLOSED RESULT-BLIND BARYCENTRE CONTROL / NO CORRELATED PRODUCTION
MEMBER, PRODUCTION SAME-MEMBER ACCEPTANCE, FULL NUMERICAL OPERATOR,
CONCRETE KILLING DIAGONAL, C0--C3, F0/F1, RELEASE, OR SUBMISSION
PROMOTION**

## 1. Exact advance and boundary

Round 185 replaces another prospective provenance layer with the smallest
ordinary-process scientific experiment that could test the level-zero
composition itself.  For the frozen `O113/Base` row it recomputes:

1. the raw Scharfetter--Gummel axis masses, directed rates, and common-flux
   intersections;
2. the physical stationary cell integrals;
3. the contact fractions and four normalized longitudinal profile bases;
4. the global gauge, factorized density ratio, conductance factors, and
   reconstructed killing-multiplier factors; and
5. the complete state and edge **topology order**, without materializing a
   dense tensor.

The program deliberately produces only a temporary exploratory receipt.  It
does not select an exact irrational point from marginal interval boxes, does
not execute the candidate-native role-v3 production chain, and does not
claim that its same-process calculation is a correlated production member.

One exact rational exploratory control is present:

```text
w = (1/4, 1/4, 1/4, 1/4).
```

It is fixed in the audited program bytes before any numerical result is read.
It is a disclosed, result-blind barycentre witness with no budget.  The
symbolic control authority itself contains no values, so the receipt
separately records that the barycentre is introduced by this exploratory
program and is not a production control.

## 2. Frozen implementation

| Object | SHA-256 | Bytes | Mode |
| --- | --- | ---: | --- |
| `code/explore_continuum_c1_n0_same_member_o113_v1.py` | `5a97d25e799162b10b432447d3d64b97dbbce3cc440bb86cb770dfa444754f86` | 61,605 | `0444` |
| `code/test_explore_continuum_c1_n0_same_member_o113_v1.py` | `6913a3b730a48cfb9a2c7deecba25ae4d5def39a823021125d91a2424fd34e8d` | 9,876 | `0444` |

The explorer pins and rechecks the exact current bytes of its scientific
authorities, all three O113 partitions, and the four imported scientific
kernel sources:

| Role | SHA-256 |
| --- | --- |
| configuration family | `063913c7fbc2b706ba85a0e3f06005bad23a2292749817294cbf41f5cdce4084` |
| reference density | `7b890d727ad0b229557de1841ae82befb8d8f83e79edc6b5348b277c3024e575` |
| ideal formula source | `f31b637b659483102d787da7263cd45c72829b3fce3df2ff9100066dec94c2be` |
| frozen factorization v1 | `70cb49e63c496d489887c764c812671b03a7352d5752f6663c377734739a1dca` |
| killing geometry | `5543f76031d731cb5bcf3e4cdf3bdabaffacb2053400e3015d6ab57906a27669` |
| symbolic control method | `fd6edf9046956d311366ff51f229523ab605d80073515b9768d5fa5cafa8904f` |
| midpoint partition | `f36127a87a19a13df6108a527a2285361be082ca81f2184e68c3c47655e7ca94` |
| relative-parallel partition | `318ee1287c419f8ed91e19e4426657e2ec28d4e367859f71373061ada7382424` |
| relative-perpendicular partition | `66f21ab3a8314dbede0f1aac6cd0bbba0738d22f75f8a7334bd79463a204d9d8` |
| role-8 formula kernel | `667855b3dc1a24f03b1c118b8c1f5f09a86850c5e359e47f345e5e66c20e2f9f` |
| role-9 integral kernel | `9b1c4867ff74e1bb0df388b093312d866f6b4fdf455110a1dd36240612ae3aca` |
| role-10 factor kernel | `321f12aa8a5df44ca9c9162704cccd0f2c526abf9577832b4824538b0afdb8e5` |
| partition kernel | `2871976855a0c598b26b8d83b33f4ea3a027a2c826ccdb2ad9b678761093e6cb` |

It also checks semantic joins rather than treating matching schemas as enough:
coordinate order; physical parameters and dimensions; authority hash edges;
configuration-to-partition geometry; transverse period and contact radius;
profile centres, half-width, count, and normalization; and the symbolic
control source's no-value/no-budget boundary.

## 3. Mathematical composition

For each axis `a`, let `mu_i^a` be the formula-defined ungauged mass and
`M_i^a` the physical stationary cell integral.  The program uses

```text
S_a             = sum_i mu_i^a
G               = M_L / (S_M S_R S_Y)
pi_h(m,r,y)     = G mu_m^M mu_r^R mu_y^Y
c_e             = G kappa_e times the two spectator-axis mu factors
M^pi(m,r,y)     = M_m^M M_r^R M_y^Y
rho(m,r,y)      = M^pi(m,r,y) / pi_h(m,r,y)
V_j(m,r,y)      = W^-1 Phi_j(m) C(r,y)
K_j(m,r,y)      = V_j(m,r,y) / rho(m,r,y).
```

The separate exact Laurent-monomial lane verifies

```text
G S_M S_R S_Y = M_L,
pi_h q = c_e,
rho pi_h = M^pi,
K = V/rho,
M^pi K = pi_h V.
```

No interval midpoint or rational endpoint is promoted to an exact
formula member.  Reported `rho`, `V`, and `K` endpoints are explicitly
enclosure-hull endpoints and are not claimed to be attained extrema.

## 4. Reproduced numerical evidence

The final clean replay produced a 29,191-byte temporary canonical receipt:

```text
SHA-256 = 2957b619caf23707dd7a6ee85f489824cbf219bedfb79febd2339c5d49b9927c
wall    = 5.63 s
max RSS = 229,048,320 bytes
```

The receipt is reproducible from the frozen code; it is not installed as a
persistent report artifact.  Its exact status states that the barycentre is
disclosed and that neither a correlated production member nor same-member
acceptance is obtained.

The recomputed factor counts are:

| Quantity | Value |
| --- | ---: |
| raw axis cells | 339 |
| physical axis cells | 339 |
| axis edge pairs | 337 |
| positive directed axis rates | 674 |
| reflecting outside-zero rates | 4 |
| 320/640-bit containment checks | 1,015 |
| contact intervals | 12,769 |
| active / full contact cells | 335 / 243 |
| profile intervals | 452 |
| tensor states | 1,442,897 |
| tensor undirected edges | 4,303,153 |
| directed off-diagonal positions | 8,606,306 |

The principal enclosure hulls are:

```text
G                 = 7.957747154594767064...  (both displayed bounds)
rho               in [0.997508357648048081..., 1.318209937258475114...]
barycentre V hull in [0, 5.029429018594628431...]
barycentre K hull in [0, 4.951873974929927165...]
```

The program independently recomputes before consulting its output-only
same-backend regression oracles.  The frozen raw-flux oracle
`04fee91f...40f3`, stationary oracle `03db61b4...7212`, and O113 role-10 row
`63ccb3bc...55a` all agree; all nine referenced raw-file pins match, and the
contact/profile arrays replay exactly.  These are regression checks, not
backend independence.

Factor and topology digests are:

```text
role-8/9 factors  f97f5695601931ca631507824352bff3752557860182adbc95578f53bdfd749d
role-10 factors   aa026bdac693340e6fc6215cbf9ad8ae1ebdc1e6e6b0f437616fbc9e013c1e12
V/K summaries    33ce99f24db6310daf75e07cb4ee84d2f3bca542f0f64208a9338f13b2fba4e1
conductance data 9e22273fa078331f08a76f288691f6e8ad3cda7b36d2a159dba1dc6ffe6b8451
state order       7b0207d3f88254442b60f3e248330865ddfcdaa5fc0e832daf803e456a35328f
edge order        b631cbfa6b86638f8762defd86807b74159436cf0c3995a9cdaa72e0ad932991
```

The conductance digest covers factorized axis/spectator data.  The state and
edge digests cover topology order only.  They are not hashes of 4,303,153
evaluated conductances, rates, or diagonals.

## 5. Adversarial chronology

The first independent input audit returned `P0=0/P1=3/P2=3`:

1. the barycentre was numerically present but not disclosed at the top-level
   claim boundary;
2. schemas and current hashes were not yet joined semantically, and imported
   kernel source hashes were absent; and
3. a gate name could be read as full numerical-operator streaming even though
   only topology was streamed.

The repair:

- sets `actual_exploratory_control_values_present=true` and
  `exploratory_barycentre_control_witness_present=true`, while retaining
  `concrete_production_control=false` and `budget_used=false`;
- pins the symbolic control source and all four imported scientific kernel
  sources, and adds the cross-source semantic joins listed above; and
- renames the gate to `full_state_and_edge_topology_orders_streamed` and
  explicitly records that no rates, conductances, or diagonals were streamed.

A post-fix mathematical audit independently rederived the gauge, ratio,
killing, and gauge-placement identities and returned
`P0=0/P1=0/P2=0`.

A separate post-fix code attack reran the 12-test suite and two isolated,
byte-identical subprocess replays.  It returned `P0=0/P1=0/P2=1`.  The
retained P2 is limited to temporary-output hardening: the path validation and
later exclusive path write are not protected against a same-user symlink
swap, and an exceptional short write can leave a truncated temporary file.
The CLI returns HOLD and never reports success in that case.  No persistent
artifact is published through this path, so this is not a scientific or
publication blocker.

## 6. Reproduction

Run from the repository root:

```bash
tmpdir=$(mktemp -d /tmp/encounter-o113-round185.XXXXXX)
env PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -I -B \
  research/reports/encounter_multimodal_prr/code/\
explore_continuum_c1_n0_same_member_o113_v1.py \
  --output "$tmpdir/receipt.json"

env PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m pytest -q \
  -p no:cacheprovider \
  research/reports/encounter_multimodal_prr/code/\
test_explore_continuum_c1_n0_same_member_o113_v1.py
```

The final focused suite is `12/12 PASS`.  It checks exact formal identities,
interval division, topology counts and seam mutation, report-tree output
rejection, two clean deterministic replays, all promotion flags, the
disclosed barycentre, literal source/kernel/oracle pins, semantic source
joins, compact output, and the absence of dense tensor payloads.

## 7. Exact stop line and next continuum path

Round 185 changes the scientific evidence ledger in one narrow way:

```text
one real O113 n=0 same-process factorized composition = PRESENT
one disclosed result-blind exploratory barycentre      = PRESENT
production correlated member / acceptance receipt     = FALSE / ABSENT
full numerical operator / concrete killed diagonal    = FALSE / FALSE
candidate-native roles 8--10 production replay         = FALSE
complete C0 / C1 / C2 / C3                             = FALSE
F0 / F1 / root transfer                                = FALSE
release / submission                                   = FALSE
```

The next scientifically useful sequence is:

1. add a nonunit-`W` fixture and an independent contact/profile evaluator;
2. bind a predeclared positive budget to the disclosed control and stream the
   actual O113 conductances, rates, diagonals, and cellwise
   `M^pi K = pi_h V` checks under the resource cap;
3. place that calculation inside the genuinely ordered, externally committed
   role-8--10 production replay and issue a distinct independent acceptance
   receipt;
4. extend the accepted construction across all twelve level-zero anchors;
5. evaluate the source-bound C2 constants, prove the positive-time C3
   box-truncation estimates, and only then attempt componentwise root
   transfer.

The report directory remains outside the current Git tracked baseline.
Round 185 therefore has exact internal hashes and live replay evidence but no
repository commit history.  The theorem-first manuscript remains unchanged:
this finite-grid exploratory result is evidence for the next continuum step,
not a new paper theorem or a reason to weaken its existing HOLD language.
