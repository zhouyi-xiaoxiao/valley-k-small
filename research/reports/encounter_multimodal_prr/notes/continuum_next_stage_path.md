# Continuum next-stage path for the theorem-first PRR branch

Date: 2026-07-17  
Scope: physical `d=2`, one broad four-slab family, one installed budget, three
already frozen exact-rational allocation vectors  
Current scientific state: the exact-`m` analytical core and reader-facing
migration remain accepted; the complete reader-facing proof passed the
hash-specific independent Round-149 audit.  Round 167 closes the 12-row control-free
source/partition/free-axis preparation; Rounds 168--170 close only the
factorized killing-geometry source and its two-repeat outer replay.
Authenticated fixed-row same-backend sources now provide physical axis-cell
integrals and formula-defined raw common fluxes, but no correlated production
member.  Round 172 defines twelve genuine ideal dyadic refinement sequences,
Round 173 supplies ideal symbolic map/cut-layer/killing inputs, and Round 174
closes their ideal fixed-box C1 composition.  No positive-`B` F1 value has
been read, and production same-member containment, F0, project-level complete
C1/C2, topology, and release remain held.

## 1. Claim ladder

The next numerical campaign may support only the first level whose complete
evidence exists.

1. **Bounded deterministic family:** the unchanged three controls have exactly
   one, two, and three maxima on the declared finite window in every required
   finite-volume configuration.
2. **Continuum-consistent bounded family:** retained root intervals, topology
   margins, basin masses, and survival admit stable mesh/parity/alignment/box
   envelopes.
3. **Independently checked event law:** an off-lattice killed-process method at
   unchanged inputs agrees on mechanically selected common-window contrasts,
   basin probabilities, and survival with simultaneous uncertainty.
4. **Focused PRR numerical result:** levels 1--3 pass without refitting and can
   be paired with the exact-`m` theorem in one physical-`d=2` narrative.
5. **Certified continuum result:** a separate discretization theorem supplies
   explicit compact-time observable and derivative errors, and converts the
   finite-volume root margins into a theorem for the unbounded continuum Doi
   law.

Levels 1--4 do not license a strict numerical continuum theorem.  None of the
levels licenses a cusp, a phase diagram, topology outside the
declared window, a useful theorem-derived value of `B0(epsilon)`, or a
positive-budget physical-`d=3` claim.

## 2. Pre-science completion boundary

Before reading any new positive-`B` control value, freeze and independently
accept all of the following:

- canonical exact-rational control bytes and their same-budget proof;
- a byte-oriented rate-defined tensor generator with exact type, ownership,
  layout, endian, length, role, and digest checks;
- packed interval storage and a bounded-memory, exact-dyadic streaming ledger;
- production-configuration reconstruction of the canonical analytic initial
  law into file-backed component boxes, with exact partition/rate hashes joined
  to packed free-axis payloads.  Round 167 closes this item at free-axis scope;
  contact killing and the full operator remain separate open requirements;
- fixed-memory block/halo actions for `P^T` and `Q^T`, with an explicit
  roundoff proof for their frozen addition order;
- a second, independently implemented local recurrence replay from canonical
  fixed input files and exact source/input hashes, with jets computed from the
  replay-owned state;
- deterministic byte comparison at each persisted artifact boundary, with
  mutation tests for every claim-bearing field.  A standard clean local
  process is sufficient for this scientific replay; no custom interprocess
  protocol is a scientific requirement;
- the common-observable selector, including runtime verification immediately
  before each numerical worker call, bounded worker output, a cross-process
  concurrency cap, and the full frozen 68-assertion power schedule;
- one result-blind clean-process replay and one independent-code-path replay,
  both bound to the same canonical manifest, together with ordinary local
  timeout and resource-cleanup tests;
- a measured resource envelope for the actual 7,165,305-state configuration on
  a host with sufficient physical memory.  Small synthetic fixtures may test
  accounting logic, but cannot substitute for this acceptance run.

Acceptance must be performed by an independent reader/test layer.  Passing the
producer's own tests is implementation progress, not authorization to read
science.

Round 152 rejected the first bounded packed directed-action bytes because the
stage-1 stride-2 Boolean `signbit` validation path misclassified valid `+0`
endpoints for large blocks on the bound runtime.  Round 154 repaired that P1
with contiguous scratch, vectorized runtime probes, and saved heterogeneous
rate/killing exact oracles.  Round 155 independently accepted the exact
repaired bytes only as a bounded implementation primitive after 99 fresh
block-size processes and 320 heterogeneous exact rows; it did not authenticate
arbitrary result payloads or supply a fresh verifier.  The stage still does not
compose interval-valued rates, implement uniformization/Poisson tails or jets,
provide independent replay, certify topology, or satisfy the actual
7,165,305-state resource gate.  F0 therefore remains open.

Round 161 subsequently accepted the current hash-specific composition/rate-
action bytes as another bounded, same-process method primitive.  A tiny
science-free uniformization layer now pins those accepted dependencies and
implements exact-rational Poisson weights, conservative tails, chained
point-ball powers, and method-diagnostic resource accounting for at most 64
states and Poisson mean at most one.  Its implementation tests pass.  The
Round-162 independent attack accepts only the exact direct producer path, with
two P2 findings that prohibit treating its recomputable ledgers as action or
exponential replay.  Generator jets, full-window topology, ordinary clean
replay, and the production resource gate remain open.  It therefore does not
change `F0`, `F1`, or manuscript status.

Round 164 now freezes three tiny successors.  The target-aware adapter handles
compact-support unit-mass component boxes and repeated chunks by adding the
complete lower-anchor l1 distance under sub-Markov contraction.  A separate
same-process semantic path reconstructs the box, canonical unit-mass witness,
dense uniformized action, exact-Fraction Poisson enclosure, and producer-ball
containment without consuming the producer action or Poisson ledgers.  The
fixed-operator jets layer encloses z0 through z4, J0 through J3, and M2 through
M4; an independent exact-Fraction stress grid includes periodic size two and
does not pretend that endpoint/midpoint sampling exhausts repeated powers over
a continuous rate box.  Independent review found P0=P1=P2=0 on the frozen
bytes.  These are still same-process, at-most-64-state method results.  The
analytic-source-to-component-box binding was still open at that round.

Round 166 now binds one canonical, control-free physical-`d=2` compact-bump
initial source to one exact `4x4x4` method partition and its deterministic
packed component bytes.  Source rederivation rejects coherent substitute
boxes.  A separate semantic implementation proves the exact tiny-grid source
witness (C-order flat components 4 and 7 equal `1/2`, all others zero), while
its directed-MPFR rectangle sums are retained only as a broad consistency
check.  The wrapper retains the certificate across two chunks, but explicitly
marks current-target lineage as unreplayed after the initial target and does
not bind the packed operator to the same axis geometry.  This does not
cover the 12 production configurations, vertex/half-volume production rows,
file-backed streaming, clean serialized whole-result/chunk-history replay,
full-window topology, or the actual 7,165,305-state resource gate; `F0`, `F1`,
and manuscript release therefore remain HOLD.

Round 167 is the living successor for that production-preparation boundary.
It covers all 12 prescribed configurations, 206 referenced files,
34,787,462 virtual states, vertex-centred endpoint half volumes, periodic
base/half shifts, a separate-source 256-bit containment check, native packed
forward/backward rate roundtrips, and two identical five-process serialized
repeats.  The separate-source path shares the `gmpy2`/MPFR backend and Simpson
remainder lemma with the producer and is not described as backend independence.
No contact-killing array, full generator, uniformization, propagated target,
root exclusion, or positive-budget value is part of that receipt.  The
largest-row resource measurement and the full F0 topology chain therefore
remain open.

Rounds 168--170 are later geometry-only successors.  They freeze,
independently reconstruct, and two-repeat outer-replay the control-free
contact/support factorization, but construct no control, budget, tensor killing
diagonal, reconstructed multiplier, or full operator.

The fixed-row physical-integral and raw-flux sources are executed through an
authenticated MPFR chain.  They remain same-backend, level-`n=0` source
evidence and do not provide backend independence or a correlated same-member
production receipt.

Historical continuum erratum: the immutable Round-165 audit accidentally wrote
an isotropic Gaussian exponent.  For
`Dmat=diag(D/2,2D,2D)` and
`b=(-gamma(z-zbar),-gamma*r_parallel,0)`, the reversible density used by the
living program and Supplemental Material is

```text
pi = Z^{-1} exp[-gamma (z-zbar)^2 / D
                -gamma r_parallel^2 / (4D)],
Z = 2 pi D W / gamma.
```

Its log-gradient is
`(-2 gamma(z-zbar)/D,-gamma*r_parallel/(2D),0)`, so multiplying
by `Dmat` gives `b`.  The Round-165 formula does not satisfy that identity and
must not be used as continuum theory evidence; its historical file and hash
remain unchanged, with Round 167 providing the explicit correction.

## 3. Sealed deterministic campaign

The production runner must start from a clean, prospective-control-free
environment and validate frozen hashes before execution.  The present working
process has read the control-side report and is therefore not an admissible
science runner.

The campaign contains exactly 36 rows: the Cartesian product of the three
fixed controls with the frozen mesh, parity, slab-alignment, and box
configurations in the protocol.  For every row, publish:

- canonical configuration and source hashes;
- interval enclosures for density, first and second time derivatives,
  survival, and basin masses;
- interval-Newton inclusions for every stationary root;
- strict curvature classification and a complete complement exclusion on the
  declared window;
- tail and mass-balance bounds;
- peak/valley contrasts and basin-mass floors;
- primary and independent-replay records with peak-memory/runtime ledgers.

One held row terminates the campaign.  Do not drop rows, replace allocations,
change windows or thresholds, reuse the historical `B=0.01` point as a rescue,
or reopen the terminal allocation-cusp branch.

## 4. Deterministic continuum analysis

If all 36 rows pass, form conservative envelopes without selecting a favorable
subsequence.

- Compare like parity first, then odd/even extrapolants.
- Treat slab-grid alignment as a discretization challenge, not a nuisance
  replicate.
- Enlarge midpoint and relative-coordinate boxes separately and jointly.
- Propagate all interval and truncation errors into root times, curvatures,
  common-window contrasts, basin masses, and survival.
- Report estimates and error bars, not Boolean gate graphics.

The deterministic outcome must state exactly which bounded/unbounded inference
is supported.  Grid refinement alone is not an independent physical solver.

## 5. Common-observable freeze and off-lattice validation

Only after the deterministic result is independently accepted:

1. run the mechanical selector once on the accepted deterministic envelopes;
2. freeze its basin cuts, comparison windows, allowances, power decisions, and
   output hashes;
3. compile and test the off-lattice state-dependent-hazard solver without
   reading its science output;
4. use a precision pool only for the frozen stopping decisions and a disjoint
   estimation pool for final reported intervals;
5. test all selected basin probabilities, survival values, and common-window
   contrasts simultaneously at the frozen familywise error budget;
6. publish seeds/counter ranges, compiler/runtime identity, sample counts,
   stopping trace, raw sufficient statistics, and replay hashes.

The off-lattice method validates event-law observables.  It does not validate
fourth derivatives, interval root exclusion, or a continuous-density `L1`
distance.

## 6. Adversarial audit rounds

Each layer receives an independent attack before promotion.

- **Theory:** quantifier order, global zero exhaustion, endpoint signs,
  slow-factor perturbation, contact-interior scope, and absence of uniform or
  observability claims.
- **Numerics:** hostile ndarray subclasses, aliasing/views/memmaps, byte-role
  swaps, endianness/layout/dtype changes, signed zero/nonfinite values,
  one-byte mutations, tie witnesses, block-boundary impulses, and bounded RSS.
- **Reproducibility:** clean-process rebuilds, exact source/manifests, no hidden
  prospective-control reads, deterministic receipts, and independently
  replayed artifacts.
- **Editorial:** every displayed sentence traced to theorem, deterministic
  envelope, or off-lattice interval; historical designs clearly separated;
  no cusp/global/continuum promotion beyond the evidence.

An attack report must distinguish a scientific failure from a verifier or
resource failure.  Either type blocks promotion, but only the former answers
the physical question.

## 7. Parallel strict-continuum theorem track

The 36-row/off-lattice chain can establish continuum-consistent evidence, but
not a certified discretization limit.  A stricter mathematical track should
therefore proceed in parallel without delaying the sealed finite-parameter
campaign:

This track is **conditional, not a default manuscript or submission blocker**.
It is activated only if the final paper claims a rigorous numerical
finite-volume-to-unbounded-continuum topology transfer.  The focused
theorem-first paper instead caps its numerical claim at finite-window,
continuum-consistent physical-`d=2` evidence and keeps C0--C3/root transfer
outside its release conjunct.

The current strict-continuum evidence is no longer a blank template:

- C0-v2/v3 freezes the exact-adjoint map semantics, global-mass gauge, positive
  cell masses, wrapped rows, and well-defined tensor preconditions, but does
  not bind the complete production application;
- Rounds 4--5 close the abstract ideal free-tensor/bounded-killing
  Mosco--resolvent and positive-time implications under their stated map and
  refinement premises;
- Round 9 refutes the standard all-discrete-pairs tensor-`Q1` `O(h)` route and
  replaces it by a one-sided regular-solution residual;
- Round 10 proves the ideal free residual, with global uniform exponent
  `alpha=1/2` forced sharply by vertex-dual endpoint half volumes; and
- Round 11 closes the ideal fixed-box mixed Neumann--periodic graph domain,
  bounded sharp-killing domain preservation, complex-sector `H2` estimate,
  rotated coercivity, and positive-time contour growth;
- Round 172 supplies the twelve genuine ideal dyadic refinement sequences; and
- Round 173 closes the symbolic map, sharp-contact cut-layer, reconstructed-
  killing, and killing-residual inputs for those ideal tails without
  numerically evaluating their constants; and
- Round 174 composes those inputs with the audited one-axis, tensor,
  bounded-killing, resolvent/Mosco, actual-initial-source, and positive-time
  bridges, closing ideal fixed-box C1 only; and
- Round 177 freezes a preproduction predecessor-authority candidate through a
  whole-directory no-replace publication under a no-hostile-writer contract,
  reconstructs all 36 partition hashes, and thereby prepares B04, but
  inventories only nine legacy kernels.  B06, external predecessor commitment,
  roles 8--10 replay, and every blocker clearance remain false.

The remaining route is therefore source-first, not another sector theorem:

1. **Candidate-native execution closure:** replace the nine outcome-bound
   legacy kernels with result-blind, parameterized role-8--10 producers and
   verifiers whose transitive code/data dependencies and exact operation DAG
   are frozen.  The current legacy data flow executes stationary role 9 before
   raw role 8 because the raw producer consumes the stationary artifact; the
   successor must split a raw primitive from that dependency or declare and
   verify the true order rather than relabel it.
2. **External predecessor commitment:** rebuild the candidate with that
   native method closure, then obtain a genuinely external commitment to the
   member, method registry, parameter registry, policy, and predecessor order.
   A locally generated review request cannot authorize its own successor.
3. **Fresh ordered production replay:** only after that commitment, generate
   fresh roles 8--10, freeze the complete executed outer manifest/operation
   model/source DAG, independently stream the exact-DAG mass/flux/gauge/map/
   killing identities, and issue a distinct acceptance receipt.  It must
   certify one correlated ideal member contained by the production intervals;
   marginal containment or geometric equality is insufficient.
4. **Project-level complete C1:** apply the accepted ideal fixed-box
   composition to the twelve Round-172 refinement families, then connect its
   level-zero member through the accepted production receipt.  An ideal-only
   Round-174 closure does not by itself close production or project C1.
5. **Numerical C2 application:** evaluate the Round-173 symbolic constants
   from frozen sources, include the separately source-bound initial-projection
   term, and compose them with the Round-10 free residual and Round-11
   sector/contour theorem for the same accepted member.
6. **C3 box truncation:** prove computable `r=0,1,2` unbounded-box errors.
7. **Root transfer:** combine unconditional C2/C3 bounds with complete
   interval root tubes, curvature and complement margins, endpoint signs, and
   event-mass floors.

The deliverable is an inequality of the form

```text
sup_control ||f_h,L/B - f_cont/B||_C2([tau,T])
    <= E_space(h, alignment) + E_box(L) + E_time/arithmetic,
```

with every term computable before inspecting topology.  Until such a bound is
available, the manuscript must use “continuum-consistent numerical evidence,”
not “continuum verified” or “proved convergence.”

The immediate deliverable is no longer another neutral source lemma or a
same-process receipt.  It is the candidate-native role-8--10 method closure
needed to clear B06, followed by a genuine external predecessor commitment,
fresh ordered replay, an independently streamed exact-DAG replay, and only
then a distinct level-`n=0` same-member acceptance receipt.  Numerical
evaluation of the Round-173 constants and the ideal-to-production C1/C2
composition come later.  Another mesh slope or neutral sector sample would
not advance either gate.

## 8. Manuscript and release fork

- If all deterministic and independent checks pass, replace the historical
  manuscript in one coherent theorem-first rewrite and populate three figures:
  geometry/exact design, same-budget one-/two-/three-mode topology, and
  convergence/independent validation.
- If the deterministic campaign holds, report the bounded negative or
  unresolved result without refitting and reassess journal fit.
- If the off-lattice comparison holds after deterministic success, retain the
  deterministic theorem/numerical result at its exact scope and do not call it
  independently validated.
- In every branch, keep the accepted exact-`m` theorem, the historical
  `B=0.01` evidence, and the failed allocation-cusp campaign as separate,
  immutable records.

Release requires a clean LaTeX build, visual PDF inspection, source and figure
hash manifests, rerunnable safe tests, explicit records of skipped production
runs, and author approval of title, overlap disclosure, acknowledgments, and
submission metadata.
