# Exact Recursion — Method Guide

**Status: WIP scaffold (not yet started).** Tracking: [#9](https://github.com/zhouyi-xiaoxiao/valley-k-small/issues/9) Directory layout is in place; no code, manuscript, or notes have been written.

## Intended scope

A self-contained methodology reference for the exact recursion estimator family (dense / sparse / defect-reduced variants) used across the ring and grid2d FPT studies. Acts as the canonical citation for methodology details, freeing individual benchmark reports from re-deriving the basics.

## Layout

- `code/` — empty
- `manuscript/` — empty
- `notes/` — empty
- `artifacts/` — empty placeholder subdirs

## TODO before this report is real

- [ ] Decide whether this is a "report" or should live under `research/docs/` as a methods note
- [ ] Land derivation + numerical-stability commentary in `manuscript/`
- [ ] Cross-link from `cross_luca_regime_map/` and `luca_vs_recursion_unified_benchmark/` once written
- [ ] Replace this README with a real one matching the style of `cross_luca_regime_map/README.md`
