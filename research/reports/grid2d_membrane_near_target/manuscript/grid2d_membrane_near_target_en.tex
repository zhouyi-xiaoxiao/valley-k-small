\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{enumitem}
\usepackage[table]{xcolor}
\usepackage{array}
\usepackage[hidelinks]{hyperref}
\usepackage{xurl}
\usepackage{microtype}

\title{2D Corridor Membranes and Near-Start Two-Target Bimodality\\\large New Extension Report Bridging Previous 2D Results}
\author{}
\date{}

\setlist{nosep}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{0pt}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.10}

\begin{document}
\maketitle

\section*{Headline Results}
This report adds two new research tracks on the 2D rectangular setting while keeping the same phase criteria and peak-valley style used in earlier reports.
\begin{enumerate}
  \item \textbf{Corridor + one target:} replace fully reflecting corridor walls by semi-permeable membranes, with both symmetric ($\kappa_{\mathrm{c\to o}}=\kappa_{\mathrm{o\to c}}$) and directional ($\kappa_{\mathrm{c\to o}}\neq\kappa_{\mathrm{o\to c}}$) cases.
  \item \textbf{No corridor + two targets:} place one target close to the start and keep the other at the far side, to test robust ``fast near-hit + slow far-hit'' splitting.
\end{enumerate}

Main outcomes:
\begin{itemize}
  \item For symmetric membranes, clear-double forms a continuous ultra-low leakage band (see Figure~\ref{fig:sym-phase} and Table~\ref{tab:sym-w16}); it then enters a weak-double transition band and finally turns mostly single-peak at higher leakage.
  \item Two directional representatives remain clear-double: easy-out/hard-return $(\kappa_{\mathrm{c\to o}},\kappa_{\mathrm{o\to c}})=(0.002,0)$ and hard-out/easy-return $(0,0.002)$. Their exact occupancy and flux diagnostics show which spatial trajectories feed peak1 versus peak2.
  \item In the no-corridor two-target setup, the mechanism-first physics anchor $(b_x,d,\Delta y)=(0.12,2,2)$ gives near/far mass $\approx0.324/0.676$ with $(t_{p1},t_v,t_{p2})\approx(7,196,469)$, while the dedicated showcase clear-instance remains $(0.12,4,1)$ with near/far mass $\approx0.350/0.650$ and valley/max $\approx0.007$.
\end{itemize}

\section{Model and Shared Baseline}
We use the existing rectangular 2D framework with reflecting boundaries:
\[
L_x=60,\quad q=0.8,\quad x_s=7,\quad x_t=58.
\]
For the corridor branch we use an updated anchor tuned for higher clear-double coverage:
\[
h=2,\quad m=5,\quad \delta_{\rm core}=1.00,\quad \delta_{\rm open}=0.55,\quad b_x=-0.08.
\]

\subsection{Membrane Definition}
Each wall-crossing attempt at a membrane edge is accepted with probability $\kappa$ and reflected with probability $1-\kappa$.
In this revision the main asymmetry is \emph{directional}: for the same membrane edge, corridor$\to$outer and outer$\to$corridor crossings can have different pass probabilities,
\[
\kappa_{\mathrm{c\to o}},\qquad \kappa_{\mathrm{o\to c}}.
\]
Symmetric case: $\kappa_{\mathrm{c\to o}}=\kappa_{\mathrm{o\to c}}=\kappa$.
Directional case: $\kappa_{\mathrm{c\to o}}\neq\kappa_{\mathrm{o\to c}}$.

\subsection{Class Labels for Mechanism}
We keep peak/valley windows and use a compact 4-class coding:
\begin{itemize}
  \item $L$: membrane leak happened or not;
  \item $R$: committor recross happened or not.
\end{itemize}
Classes are \texttt{L0R0}, \texttt{L0R1}, \texttt{L1R0}, \texttt{L1R1}.

\section{Track A: Corridor + One Target + Membranes}
\subsection{Symmetric Scan}
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_symmetric_phase_map.pdf}
  \caption{Symmetric membrane phase map (rows: $W_y$, columns: $\kappa$). The colorbar is discrete: phase $\in\{0,1,2\}$ only.}
  \label{fig:sym-phase}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_symmetric_sep_map.pdf}
  \caption{Symmetric membrane separation map (sep-score).}
  \label{fig:sym-sep}
\end{figure}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \input{tables/membrane_symmetric_w16.tex}
  \caption{Symmetric membrane metrics on the $W_y=16$ slice.}
  \label{tab:sym-w16}
\end{table}

In this run (taking the $W_y=16$ slice as reference), strong bimodality gradually weakens with permeability: clear-double for $\kappa\in[0,0.0035]$, weak-double around $\kappa\in[0.0037,0.02]$, and mostly single-peak for $\kappa\ge 0.03$.

\subsection{Directional Scan}
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_directional_phase_map.pdf}
  \caption{Directional membrane phase map ($\kappa_{\mathrm{c\to o}}$ vs $\kappa_{\mathrm{o\to c}}$). The colorbar is discrete: phase $\in\{0,1,2\}$ only.}
  \label{fig:asym-phase}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_directional_sep_map.pdf}
  \caption{Directional membrane separation map.}
  \label{fig:asym-sep}
\end{figure}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \input{tables/membrane_directional_topcases.tex}
  \caption{Top directional membrane cases (ranked by phase and separation).}
  \label{tab:asym-top}
\end{table}

Representative overlays:
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/membrane_rep_sym_geometry.pdf}
  \caption{Symmetric membrane representative geometry (corridor band, start/target, local-bias direction). The two dashed horizontal lines are the two semi-permeable membrane boundaries; in the symmetric case they share the same directional permeability $\kappa_{\mathrm{c\to o}}=\kappa_{\mathrm{o\to c}}=0.002$.}
  \label{fig:rep-sym-geo}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.84\linewidth]{figures/one_target_basin_schematic.pdf}
  \caption{Exact one-target committor geometry used in the strict recross definition. The figure shows the start basin $A=\{x\le x_s+1,\ |y-y_s|\le 1\}$, the target set $B$, and the commitment surface built from $q=0.5$.}
  \label{fig:one-target-basin}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_rep_sym_fpt.pdf}
  \caption{Symmetric representative ($\kappa=0.002$): total FPT and last-hop source decomposition. In this geometry the outer-source branch is identically zero, because every source state adjacent to the target lies inside the corridor.}
  \label{fig:rep-sym-fpt}
\end{figure}

Figure~\ref{fig:one-target-basin} is now placed next to the representative geometry so the basin/committor picture is visible before the class-decomposition discussion starts. The green region is the strict-recross start basin, and the white contour is the $q=0.5$ commitment surface.

To answer the concrete question ``when does bimodality disappear as the red start point moves up/down/left/right?'', we now scan $(x_s,y_s)$ over the full reflecting box. The black box marks the original start, and the dashed gray T cell marks the target location itself (excluded as an admissible start); any cell where phase drops from 2 to 1/0 is a loss of the clear double-peak regime.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/one_target_start_phase_map.pdf}
  \caption{Global one-target phase map under moved start positions. The axes are the moved $x_s,y_s$; the black box marks the original start, T marks the target cell, and A/B/C mark three key phase0 exemplars. Phase only takes the discrete values $\{0,1,2\}$, so the plot is rendered with discrete color blocks rather than a continuous gradient.}
  \label{fig:one-target-start-phase}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/one_target_start_phase0_atlas.pdf}
  \caption{Geometry schematics for the three phase0 exemplars A/B/C from Figure~\ref{fig:one-target-start-phase}. A is the earliest centerline failure point $(46,7)$; B is an off-axis failure point near the target $(47,5)$; C is the target-adjacent collapse point $(57,7)$. Together they show that the one-target double peak does not die first near the original red point; phase0 appears only after the start is pushed into the right-side near-target band.}
  \label{fig:one-target-start-phase0-atlas}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/one_target_start_sep_map.pdf}
  \caption{Local-neighborhood separation score around the original red point. It complements the discrete phase map by showing how the peak-valley structure weakens before or after a phase drop.}
  \label{fig:one-target-start-sep}
\end{figure}

The full scan shows that phase0 does not appear near the original start. Instead it is confined to a right-side band in front of the target: the nearest phase0 point is already $(46,7)$, and the full phase0 region occupies approximately $x_s\in[46,59]$ and $y_s\in[3,11]$. So the one-target double peak first degrades from clear-double to weak-double (phase 2 $\to$ 1); only when the start is pushed almost onto the target side does the second peak disappear completely.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/membrane_rep_dir_out_geometry.pdf}
  \caption{Directional representative with easy-out / hard-return $(\kappa_{\mathrm{c\to o}},\kappa_{\mathrm{o\to c}})=(0.002,0)$. The geometry is unchanged; only the two crossing directions on the same membrane are no longer equivalent.}
  \label{fig:rep-asym-geo}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_rep_dir_out_fpt.pdf}
  \caption{Easy-out / hard-return representative: total FPT and last-hop source decomposition. Here again the outer-source branch is identically zero for geometric reasons, so the useful information is the shifted peak-valley timing under directional permeability.}
  \label{fig:rep-asym-fpt}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/membrane_rep_dir_in_fpt.pdf}
  \caption{Hard-out / easy-return representative $(\kappa_{\mathrm{c\to o}},\kappa_{\mathrm{o\to c}})=(0,0.002)$, shown for contrast with Figure~\ref{fig:rep-asym-fpt}.}
  \label{fig:rep-dir-in-fpt}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/membrane_class_window_bars.pdf}
  \caption{Window-wise composition of 4 classes (L$\times$R), used here as a consistency check rather than the primary spatial diagnostic.}
  \label{fig:membrane-class-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/membrane_class_legend.pdf}
  \caption{Representative L$\times$R legend: left, 2x2 class definition; right, typical dominant-class transfer across peak1/valley/peak2 windows.}
  \label{fig:membrane-class-legend}
\end{figure}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \input{tables/membrane_window_classes.tex}
  \caption{Numerical values of the 4-class window composition.}
  \label{tab:membrane-window-classes}
\end{table}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \input{tables/membrane_qstar_sensitivity.tex}
  \caption{$q^*$ robustness check (exact decomposition): dominant class and $w_{\mathrm{L1R1}}$ across thresholds.}
  \label{tab:membrane-qstar-sens}
\end{table}

With strict recross (visit $\Sigma_{q^*}$ then return to $A$), the exact decomposition shows: peak1 is \texttt{L0R0}-dominated, valley remains mostly \texttt{L0R0} with increased \texttt{L1R0}, and peak2 is \texttt{L0R1}-dominated. So the late peak is mainly a commitment-failure effect, not necessarily leak-driven.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/membrane_directional_window_occupancy_atlas.pdf}
  \caption{Exact pre-hit occupancy conditioned on eventually landing in peak1 / valley / peak2 windows. The two rows compare easy-out / hard-return and hard-out / easy-return directional membranes.}
  \label{fig:membrane-dir-occ}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/membrane_directional_window_flux.pdf}
  \caption{Exact directional membrane flux conditioned on each window. The bars show expected corridor$\to$outer versus outer$\to$corridor crossings per window-hit trajectory, making the peak1/peak2 asymmetry explicit.}
  \label{fig:membrane-dir-flux}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/membrane_directional_trajectory_atlas.pdf}
  \caption{Small MC trajectory atlas aligned to peak1 and peak2 windows for the two directional representatives. These panels are illustrative only; the quantitative claims come from the exact diagnostics in Figures~\ref{fig:membrane-dir-occ} and \ref{fig:membrane-dir-flux}.}
  \label{fig:membrane-dir-mc}
\end{figure}

\section{Track B: No Corridor + Two Targets (Near-Start Target)}
We remove corridor geometry and keep reflecting boundaries only. One target is placed near the start, and Track B is now organized around two distinct anchors: a \emph{physics anchor} $(b_x,d,\Delta y)=(0.12,2,2)$ for mechanism analysis, and a \emph{showcase clear-instance} $(0.12,4,1)$ for the sharpest double-peak visualization. The old coarse $(d,b_x)$ scan is still kept as background, but the narrative below follows the order ``geometry $\rightarrow$ branch splitting $\rightarrow$ continuation $\rightarrow$ loss modes'' so we do not classify a disappearing second peak before checking what physically disappeared.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/two_target_geometry_atlas.pdf}
  \caption{General no-corridor geometry atlas. The four panels show the physics anchor clear-double, the showcase clear-instance, a near-overcapture loss, and a misaligned near-target loss. We only draw reflecting-box geometry, drift arrows, and target annotations, so the schematic no longer resembles a membrane barrier.}
  \label{fig:tt-geometry-atlas}
\end{figure}

The atlas in Figure~\ref{fig:tt-geometry-atlas} gives the qualitative vocabulary used in the rest of Track B. The physics anchor is the route we use to interpret where the second peak comes from; the showcase clear-instance is kept separately because it maximizes visual sharpness rather than explanatory simplicity.

At the physics anchor $(b_x,d,\Delta y)=(0.12,2,2)$, the near/far splitting is already clean: $P_{\rm near}\approx0.324$, $P_{\rm far}\approx0.676$, and the three characteristic times are $(t_{p1},t_v,t_{p2})=(7,196,469)$. So the basic mechanism is not ``one peak plus a tail shoulder''; it is an honest competition between a fast near-hit branch and a slower far-hit branch with well-separated conditional time scales.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/two_target_rep_geometry.pdf}
  \caption{Physics-anchor geometry (legacy ``representative'' file name): reflecting box, start, near target, far target, and global drift direction.}
  \label{fig:tt-near-rep-geo}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{figures/two_target_rep_fpt.pdf}
  \caption{Physics-anchor total FPT together with the exact near/far splitting. Peak1 is near-dominated and peak2 is far-dominated.}
  \label{fig:tt-near-rep-fpt}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_rep_doublepeak_example.pdf}
  \caption{Physics-anchor double-peak visualization: a tail-emphasized linear view and a log view. The second peak remains explicit in both views.}
  \label{fig:tt-near-doublepeak-example}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.82\linewidth]{figures/two_target_rep_window_split.pdf}
  \caption{Window-wise near/far mass fractions in the physics-anchor case. Peak1 stays near-dominated, the valley is mixed, and peak2 is far-dominated.}
  \label{fig:tt-near-window}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.92\linewidth]{figures/two_target_committor_surface.pdf}
  \caption{Physics-anchor splitting committor field. The heatmap value is $q_{\rm far}(x)=\mathbb P_x(\tau_{\rm far}<\tau_{\rm near})$, and the single black contour is the commitment surface $\Sigma_{0.5}=\{q_{\rm far}=0.5\}$. It separates near-dominant and far-dominant regions and gives a geometric picture for the branch split.}
  \label{fig:tt-committor-surface}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_physics_env_heatmap.pdf}
  \caption{Physics-anchor conditional occupancy and environment heatmaps. They show the fast near-hit and slow far-hit families as two distinct dynamical routes rather than two plotting artifacts of the same family.}
  \label{fig:tt-physics-env}
\end{figure}

The splitting committor in Figure~\ref{fig:tt-committor-surface} closes the mechanism loop: $q_{\rm far}(x_s)=0.676009$ agrees with the exact far-hit mass $P_{\rm far}=0.676009$ to numerical precision, and the $q_{\rm far}=0.5$ contour gives a literal dividing surface between near-dominant and far-dominant regions. This is why we now describe the second peak as a far-branch phenomenon before talking about any phase label.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_start_peak_branch.pdf}
  \caption{Peak-branch continuation under start motion. We compare \texttt{source\_only\_x} against the control family \texttt{source\_plus\_near\_x}, where the near target is translated together with the source.}
  \label{fig:tt-start-branches}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_start_mass_budget.pdf}
  \caption{Start-scan mass budget. We track $P_{\rm near}$, $P_{\rm far}$, the peak-window masses $M_1/M_2$, and the ordered loss-mode strip.}
  \label{fig:tt-start-mass}
\end{figure}

Figures~\ref{fig:tt-start-branches} and \ref{fig:tt-start-mass} answer the first continuation question. When only the source is moved in $x$ while the near target is fixed, only $4/11$ positions remain \texttt{clear}; the remaining rows are mostly \texttt{near\_mass\_loss} ($5$ cases) plus a smaller \texttt{far\_broadening} sector ($2$ cases). In contrast, the comoving control \texttt{source\_plus\_near\_x} stays \texttt{clear} for all $11$ positions. So the second peak is not primarily lost by moving the source per se; it is lost when source motion changes the global source-near-boundary geometry and suppresses the early near branch.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/two_target_near_lossmode_bx012.pdf}
  \caption{Near-target loss-mode map at $b_x=0.12$ over $(d,\Delta y)$. The phase-2 boundary is overlaid on top of the ordered loss-mode classification. The colorbar follows the full six-label classifier, but only \texttt{clear}, \texttt{near\_mass\_loss}, and \texttt{far\_broadening} are populated in this scan window; the numbers in parentheses give the per-label counts for this panel.}
  \label{fig:tt-loss-bx012}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/two_target_near_lossmode_bx014.pdf}
  \caption{Near-target loss-mode map at $b_x=0.14$. Increasing the forward bias shrinks the robust region mainly through the same near-branch failure mode; although the colorbar keeps all six labels for consistency, only three are activated in this panel as well.}
  \label{fig:tt-loss-bx014}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_failure_env_heatmap.pdf}
  \caption{Failure exemplar at $(b_x,d,\Delta y)=(0.12,2,6)$, classified as \texttt{near\_mass\_loss}. The slow far branch remains, but the fast near branch loses enough weight that the clear-double case disappears.}
  \label{fig:tt-loss-env}
\end{figure}

The near-target position scan addresses the second question directly. Over the full $(d,\Delta y)$ map, $67/77$ cases remain phase 2 at $b_x=0.12$, while $59/77$ remain phase 2 at $b_x=0.14$. Across both slices the dominant failure mode is \texttt{near\_mass\_loss} (22 cases in total), with only six \texttt{far\_broadening} cases and no \texttt{tail\_truncation\_risk} after the adaptive-horizon guard. The failure exemplar in Figure~\ref{fig:tt-loss-env} makes the interpretation concrete: moving the near target too far off alignment destroys the fast branch before it destroys the slow branch.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/two_target_nearstart_phase_map.pdf}
  \caption{Coarse no-corridor phase map (near-target distance $d$ vs $b_x$) retained as a global reference. The colorbar is discrete: phase $\in\{0,1,2\}$ only.}
  \label{fig:tt-near-phase}
\end{figure}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \input{tables/two_target_nearstart_summary.tex}
  \caption{Background summary of the original $(d,b_x)$ scan: clear intervals and best near-target distance per $b_x$.}
  \label{tab:tt-near-summary}
\end{table}

\begin{table}[!htbp]
  \centering
  \footnotesize
  \resizebox{\linewidth}{!}{\input{tables/two_target_no_corridor_clear_instance.tex}}
  \caption{No-corridor clear-instance quality metrics (auto-selected from all candidate scans). We report \texttt{peak-margin}, \texttt{min-margin}, and strong-gate audit fields (\texttt{gate-pass}/\texttt{gate-slack}) to avoid near-threshold pseudo-clear picks, and include \texttt{showcase} for visual ranking consistency.}
  \label{tab:tt-near-clear-instance}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{figures/two_target_no_corridor_clear_instance.pdf}
  \caption{Dedicated no-corridor showcase clear-instance (maximum showcase score over all candidate scans): tail-emphasized linear panel plus log panel to keep valley/peak2 structure explicit.}
  \label{fig:tt-near-clear-instance}
\end{figure}

The clear-instance metrics (near/far masses, sep-score, valley/max, peak/min margins, showcase score, and splitting-committor closure error $\varepsilon_{\rm split}$) are listed in Table~\ref{tab:tt-near-clear-instance}. The selected showcase case comes from the adaptive-refinement round, $(b_x,d,\Delta y)=(0.12,4,1)$, with \texttt{peak-margin}=0.070, \texttt{min-margin}=0.070, and \texttt{showcase}=1.92. It passes the first robustness gate
\[
\mathrm{peak\mbox{-}margin}\ge 0.07,\qquad \mathrm{min\mbox{-}margin}\ge 0.07,\qquad \mathrm{valley}/\max\le 0.10.
\]
Its primary-gate audit reports \texttt{gate-pass=yes}, \texttt{gate-slack}=0.070 (minimum slack over the three thresholds), and 168 candidates in the first gate pool. We keep this point as the sharpest picture, but all mechanism claims in Track B are anchored at $(0.12,2,2)$.

\section{Method Reference (Committor + 2x2 Classes)}
\subsection*{Reproducible Definition of Phase and sep-score}
To make phase maps fully reproducible, we use the same criteria as in \path{src/vkcore/grid2d/rect_bimodality/cli.py}:
\paragraph{(1) Two-peak detection (\texttt{find\_two\_peaks})}
Let $\tilde f=\mathrm{smooth}_7(f)$ (window-7 smoothing). Candidate peaks satisfy
\[
\tilde f(t)>\tilde f(t-1),\quad \tilde f(t)\ge \tilde f(t+1),\quad \tilde f(t)\ge 0.06\,\max \tilde f.
\]
Pair selection then enforces (default code thresholds)
\[
t_{p2}-t_{p1}\ge 20,\ \mathrm{peak\_balance}\ge 0.08,\ \mathrm{valley\_drop}\ge 0.08,\ \mathrm{minor\_drop}\ge 0.005,
\]
plus side-dominance tolerance (\texttt{dominance\_tol}=0.02). Define
\[
\mathrm{has\_double}:=\mathbf 1\{t_{p1},t_{p2}\ \text{both exist}\}.
\]
These front-end conditions already form the first anti-fake filter:
\begin{itemize}
  \item Smoothing $\tilde f=\mathrm{smooth}_7(f)$ suppresses parity oscillations, staircase artifacts, and tiny numerical ripples, so discrete noise is not promoted into a second peak.
  \item The relative threshold $0.06\,\max\tilde f$ removes very small tail bumps; if a would-be second peak is below $6\%$ of the main peak, it is more naturally treated as tail roughness than as a genuine lobe.
  \item The time-gap condition $t_{p2}-t_{p1}\ge 20$ rejects shoulder structures on a single broad peak; without enough separation, two local maxima are usually just one lobe with a wrinkle.
  \item Candidate-level balance $\mathrm{peak\_balance}\ge 0.08$ rejects extremely weak secondary ripples, so a tiny bulge next to a main peak is not counted as a second peak.
  \item $\mathrm{valley\_drop}\ge 0.08$ enforces a nontrivial valley below the higher peak and removes shallow-notch pseudo-splitting.
  \item $\mathrm{minor\_drop}\ge 0.005$ further requires the valley to sit meaningfully below the weaker peak itself, specifically targeting near-flat plateaus or light waviness that can masquerade as a second peak.
  \item Side-dominance tolerance forces each selected peak to be near-dominant on its own side of the valley, which blocks the common shoulder-artifact failure mode.
\end{itemize}
So the later phase labels operate only after this first pass has already removed most obvious fake double peaks.

\paragraph{(2) One-target phase definition}
Define
\[
\mathrm{sep}_{\rm peaks}:=\frac{|t_{p2}-t_{p1}|}{w_{1/2}^{(1)}+w_{1/2}^{(2)}},\quad
\mathrm{valley}/\max:=\frac{h_v}{\max(h_{p1},h_{p2})},\quad
\mathrm{peak\_balance}:=\frac{\min(h_{p1},h_{p2})}{\max(h_{p1},h_{p2})}.
\]
Here $w_{1/2}^{(k)}$ is half-width at half-maximum of peak $k$ (computed on raw $f$), while $h_{p1},h_{p2},h_v$ are read from smoothed $\tilde f$.
Then
\[
\mathrm{phase}_{\rm OT}=
\begin{cases}
2,& \mathrm{has\_double}=1,\ \mathrm{sep}_{\rm peaks}\ge 1.0,\ \mathrm{valley}/\max\le 0.35,\ \mathrm{peak\_balance}\ge 0.15,\\
1,& \mathrm{has\_double}=1\ \text{but the above clear-double test fails},\\
0,& \mathrm{has\_double}=0.
\end{cases}
\]
The role of each threshold is:
\begin{itemize}
  \item $\mathrm{has\_double}=1$ is the basic prerequisite: if the front-end detector does not find two credible peaks, the case cannot be called clear-double.
  \item $\mathrm{sep}_{\rm peaks}\ge 1.0$ requires the distance between the two peak centers to be at least comparable to the sum of their half-widths; this blocks ``one broad peak with a small wiggle'' and heavily overlapping two-lobe shapes.
  \item $\mathrm{valley}/\max\le 0.35$ requires a sufficiently deep valley; this blocks shallow-notch or double-shoulder shapes that are visually close to a single peak.
  \item $\mathrm{peak\_balance}\ge 0.15$ requires the weaker peak to retain at least $15\%$ of the stronger peak height; this blocks cases where the ``second peak'' is only a tiny bump trailing the main peak.
\end{itemize}
So for one-target runs, \textbf{phase=2} is a deliberately shape-based clear-double label: two peaks must be separated, the valley must be real, and the weaker peak must still be non-negligible.

\paragraph{(3) Two-target phase definition}
Define modal times and mode-separation score
\[
t_{\rm mode}^{\rm near}:=\arg\max_t f_{\rm near}(t),\quad
t_{\rm mode}^{\rm far}:=\arg\max_t f_{\rm far}(t),
\]
\[
\mathrm{sep}_{\rm mode}:=\frac{|t_{\rm mode}^{\rm far}-t_{\rm mode}^{\rm near}|}{w_{1/2}^{\rm far}+w_{1/2}^{\rm near}}.
\]
Define normalized splitting masses
\[
P_{\rm near}:=\frac{\sum_t f_{\rm near}(t)}{\sum_t f_{\rm any}(t)},\qquad
P_{\rm far}:=\frac{\sum_t f_{\rm far}(t)}{\sum_t f_{\rm any}(t)}.
\]
On the total law, define
\[
\mathrm{peak\_ratio}:=\frac{\min(h_{p1},h_{p2})}{\max(h_{p1},h_{p2})},\qquad
\mathrm{valley}/\max:=\frac{h_v}{\max(h_{p1},h_{p2})}.
\]
Then
\[
\mathrm{phase}_{\rm TT}=
\begin{cases}
2,& \mathrm{has\_double}=1,\ \mathrm{sep}_{\rm mode}\ge 1.0,\ \min(P_{\rm near},P_{\rm far})\ge 0.15,\\
& \mathrm{valley}/\max\le 0.35,\ \mathrm{peak\_ratio}\ge 0.10,\ (\mathrm{peak\_ratio}-\mathrm{valley}/\max)\ge 0.01,\\
1,& \mathrm{has\_double}=1\ \text{but the above clear-double test fails},\\
0,& \mathrm{has\_double}=0.
\end{cases}
\]
The two-target gate is stricter than the one-target gate because here we can test not only total-curve shape but also the physical near/far branch split:
\begin{itemize}
  \item $\mathrm{has\_double}=1$ is still required at the level of the total FPT curve; if the total law does not even look two-peaked, the case cannot be clear-double.
  \item $\mathrm{sep}_{\rm mode}\ge 1.0$ compares the modal times and widths of $f_{\rm near}$ and $f_{\rm far}$ themselves, not just the total curve; this blocks cases where the total curve has a shallow notch even though the two physical branches do not have genuinely separated time scales.
  \item $\min(P_{\rm near},P_{\rm far})\ge 0.15$ forces both branches to carry nontrivial mass; this blocks ``one real branch plus one tiny tail branch'' from being promoted into a true double-peak claim.
  \item $\mathrm{valley}/\max\le 0.35$ again enforces a real valley in the total curve and rejects shallow-notch shapes.
  \item $\mathrm{peak\_ratio}\ge 0.10$ requires the weaker peak in the total law to stay above $10\%$ of the stronger one; this blocks extremely small secondary bumps.
  \item $(\mathrm{peak\_ratio}-\mathrm{valley}/\max)\ge 0.01$ further requires the weaker peak to stand measurably above the valley floor; this specifically blocks plateau-like or lightly wavy shapes where the ``second peak'' is barely above the intervening valley.
\end{itemize}
So mathematically, \textbf{phase=1} means ``double peaks are detected, but at least one clear-double threshold is not met'', while \textbf{phase=2} in the two-target setting should be read as a joint statement about total-curve shape \emph{and} physically separated near/far branches.

\subsection*{Adaptive Horizon Guard and Loss-Mode Rules}
For Track B continuation scans we do not infer ``the second peak disappeared'' from a fixed horizon. We start from $t_{\max}=12000$ and double it until both
\[
\mathrm{survival\_tail}\le 10^{-12},
\qquad
t_{\mathrm{mode,far}} < 0.85\, t_{\max},
\]
or until the hard cap $t_{\max}=48000$ is reached. Any row that still violates the guard is labeled \texttt{tail\_truncation\_risk}. The ordered loss-mode classifier is then applied as
\[
\texttt{clear}\ \rightarrow\ \texttt{far\_mass\_loss}\ \rightarrow\ \texttt{near\_mass\_loss}\ \rightarrow\ \texttt{timescale\_merge}\ \rightarrow\ \texttt{far\_broadening}\ \rightarrow\ \texttt{tail\_truncation\_risk},
\]
with the following concrete meanings:
\begin{itemize}
  \item \texttt{clear}: the row still satisfies the full two-target clear-double gate, i.e., phase=2.
  \item \texttt{far\_mass\_loss}: $P_{\rm far}<0.15$, so the slow far branch loses too much total mass to support the second peak.
  \item \texttt{near\_mass\_loss}: $P_{\rm near}<0.15$, so the fast near branch loses mass first and the early peak collapses.
  \item \texttt{timescale\_merge}: both masses remain nontrivial, but $\mathrm{sep}_{\rm mode}<1.0$ or $t_{\rm mode}^{\rm far}-t_{\rm mode}^{\rm near}<60$, meaning the near/far conditional timescales have merged and the valley is no longer sharp.
  \item \texttt{far\_broadening}: none of the earlier rules fires, but the far branch has broadened enough to smear out the slow peak; physically, the late branch still exists in mass but no longer produces a clean peak top.
  \item \texttt{tail\_truncation\_risk}: even at $t_{\max}=48000$ the horizon guard is not passed, so a missing second peak must still be treated as a time-window truncation risk rather than a physical loss.
\end{itemize}
So a lost case is first interpreted in terms of branch mass and branch width before being called a classification failure. In the current start and near-target continuation scans, all rows pass the horizon guard; the dominant loss mode is therefore physical (\texttt{near\_mass\_loss}), not numerical truncation. In the present $(d,\Delta y)$ window, only \texttt{clear}, \texttt{near\_mass\_loss}, and \texttt{far\_broadening} are actually populated, so the maps display only three colors even though the unified classifier keeps six labels.

\subsection*{Committor Mechanism (Detailed)}
Let the hitting time be $\tau_C:=\inf\{t\ge 0:\,X_t\in C\}$. On the discrete Markov chain with transition matrix $P$, let $A$ be the start basin (here a strip near the start) and $B$ be the absorbing target set. Define
\[
q_i := \mathbb P_i(\tau_B < \tau_A),
\]
which solves the discrete Dirichlet boundary-value problem
\[
q_i=0\ (i\in A),\qquad q_i=1\ (i\in B),\qquad
q_i=\sum_j P_{ij} q_j\ (i\notin A\cup B).
\]
With $U=\Omega\setminus(A\cup B)$, this is equivalent to the linear system
\[
(I-P_{UU})\,q_U=P_{UB}\,\mathbf 1.
\]
For this absorbing setup the solution is unique, so $q$ is a dynamics-determined progress coordinate (not an ad hoc geometric proxy).
We use $q$ as the progress coordinate and set $q^*=0.5$ to define the commitment surface
\[
\Sigma_{q^*}:=\{i:\ q_i\ge q^*\}.
\]
Moreover, when $\rho(P_{UU})<1$, the Neumann expansion gives
\[
q_U=(I-P_{UU})^{-1}P_{UB}\mathbf 1
=\sum_{n\ge 0}P_{UU}^n P_{UB}\mathbf 1.
\]
So $q_U$ is the cumulative contribution of paths that spend $n$ interior steps before absorption into $B$, which provides a direct linear-system bridge to the renewal failure-retry interpretation used later: early peak mass is small-$n$ dominated, while the slow peak accumulates from larger-$n$ loops.

\subsection*{L$\times$R Events and Recross Counts}
For a trajectory $X_t$ up to $\tau_B$, define membrane-cross count
\[
N_{\rm mem}:=\sum_{t=0}^{\tau_B-1}\mathbf 1\!\{(X_t,X_{t+1})\in\mathcal E_{\rm mem}\},
\]
and commitment-to-start recross count
\[
N_{\rm ret}:=\sum_{t=0}^{\tau_B-1}\mathbf 1\!\{X_t\in\Sigma_{q^*},\,X_{t+1}\in A\}.
\]
Then use binary labels
\[
L=\mathbf 1\{N_{\rm mem}\ge 1\},\qquad R=\mathbf 1\{N_{\rm ret}\ge 1\},
\]
which yields four classes
\[
(\!L,R\!)\in\{(0,0),(0,1),(1,0),(1,1)\},
\]
implemented as \texttt{L0R0/L0R1/L1R0/L1R1} (Figure~\ref{fig:membrane-class-legend}).

\subsection*{Peak/Valley Mechanism as Renewal Decomposition}
Let $f(t)$ be the FPT law. With direct-success kernel $g(t)$ and fail-return kernel $h(t)$,
\[
f(t)=g(t)+(h*f)(t)=\sum_{k\ge 0}(h^{*k}*g)(t).
\]
Here $k$ is the number of failed attempts before success. Bimodality appears when the $k=0$ (near one-shot) and $k\ge1$ parts separate in both timescale and mass. Window fractions
\[
w_{\ell r}(W)=\frac{\sum_{t\in W} f_{\ell r}(t)}{\sum_{t\in W} f(t)}
\]
make the transfer explicit: peak1 is mainly \texttt{L0R0}; valley remains \texttt{L0R0}-major with a clear rise of \texttt{L1R0}; and peak2 is \texttt{L0R1}-dominant, with \texttt{L1R1} secondary (Figure~\ref{fig:membrane-class-bars}, Table~\ref{tab:membrane-window-classes}).

\subsection*{Closed-Form Bridge from Linear System to FPT Kernels}
For the no-corridor two-target branch, let $U$ denote interior transient states, $e_s$ the interior basis vector at the start, and
\[
Q:=P_{UU},\qquad R_{\rm near}:=P_{U,\partial_{\rm near}},\qquad R_{\rm far}:=P_{U,\partial_{\rm far}}.
\]
Then the first-hit pmfs are
\[
f_{\rm near}(t)=e_s^\top Q^{t-1}R_{\rm near}\mathbf 1,\qquad
f_{\rm far}(t)=e_s^\top Q^{t-1}R_{\rm far}\mathbf 1,\qquad t\ge 1.
\]
Hence the far-hit mass is
\[
P_{\rm far}=\sum_{t\ge 1}f_{\rm far}(t)=e_s^\top(I-Q)^{-1}R_{\rm far}\mathbf 1.
\]
This gives the exact bridge between time-domain decomposition and the linear-system view used by committors.

\subsection*{Conditioned Kernel (Doob Transform) and Peak Transfer}
On interior states with $q_{\rm far}(i)>0$, define
\[
\widetilde P^{\rm far}_{ij}:=\frac{P_{ij}\,q_{\rm far}(j)}{q_{\rm far}(i)}.
\]
This is the Doob $h$-transform of $P$ by $h=q_{\rm far}$, i.e., the exact dynamics conditioned on eventually hitting \emph{far} before \emph{near}. In this view, the near branch is the complementary conditioned dynamics through $q_{\rm near}=1-q_{\rm far}$. Therefore, the window transfer from peak1 to peak2 can be interpreted as a measure transfer between two conditioned ensembles: early-time mass is dominated by short successful excursions, while late-time mass is amplified by loops that repeatedly leave and re-enter low-$q_{\rm far}$ regions before final absorption. This provides a direct dynamical meaning for the observed renewal split.

\subsection*{Spectral View: Origin of Fast/Slow Timescales}
On the interior kernel $Q$, write the (generalized) spectral expansion
\[
Q^{t-1}=\sum_k \lambda_k^{\,t-1} r_k\ell_k^\top,\qquad |\lambda_1|>|\lambda_2|\ge\cdots.
\]
Then
\[
f_{\rm near}(t)=\sum_k c_k^{\rm near}\lambda_k^{\,t-1},\qquad
f_{\rm far}(t)=\sum_k c_k^{\rm far}\lambda_k^{\,t-1}.
\]
Modes with smaller $|\lambda_k|$ decay quickly and dominate early windows, while the leading mode $\lambda_1$ controls the slow tail and late peak. If near/far branches project differently on these modes, we naturally obtain ``near-dominant early window'' versus ``far-dominant late window'' transfer. This is fully consistent with the renewal picture: peak1 is short-stay/low-loop mass, while peak2 is quasi-stationary release from the leading mode. Equivalently, the conditioned hazard
\[
h_{\rm far}(t):=\frac{f_{\rm far}(t)}{S(t-1)}
\]
increases over time when the far channel progressively takes over late windows.

\subsection*{Splitting Committor Closure for Two Targets}
On the same system, define
\[
q_{\rm far}(i):=\mathbb P_i(\tau_{\rm far}<\tau_{\rm near}),\qquad q_{\rm near}(i)=1-q_{\rm far}(i).
\]
Its boundary-value problem is
\[
q_{\rm far}=0\ \text{on near},\qquad q_{\rm far}=1\ \text{on far},\qquad
q_{\rm far}(i)=\sum_j P_{ij}\,q_{\rm far}(j)\ \text{on interior}.
\]
This gives a direct closure with the exact decomposition $f=f_{\rm near}+f_{\rm far}$: $q_{\rm far}(x_s)$ should match $P_{\rm far}$. Define
\[
\varepsilon_{\rm split}:=\left|q_{\rm far}(x_s)-\sum_{t\ge 1}f_{\rm far}(t)\right|
=\left|q_{\rm far}(x_s)-P_{\rm far}\right|.
\]
Moreover, let $\tau:=\tau_{\rm near}\wedge\tau_{\rm far}$ and define
\[
M_t:=q_{\rm far}(X_{t\wedge\tau}).
\]
Because $q_{\rm far}$ is harmonic on interior states and fixed to $0/1$ on near/far boundaries, $M_t$ is a bounded martingale. Optional stopping then gives
\[
q_{\rm far}(x_s)=\mathbb E[M_\tau]
=\mathbb P_{x_s}(\tau_{\rm far}<\tau_{\rm near})
=P_{\rm far}.
\]
So $\varepsilon_{\rm split}$ is theoretically zero; numerically, its scale should track the Bellman-residual scale, making it a joint diagnostic for mechanism closure and solver stability.
\begin{table}[!htbp]
  \centering
  \footnotesize
  \resizebox{\linewidth}{!}{\input{tables/two_target_splitting_committor_qc.tex}}
  \caption{Splitting-committor closure QC for no-corridor two-target runs (physics anchor, listed as ``representative'' in the legacy table, plus dedicated clear-instance): we report $q_{\rm far}(x_s)$, $P_{\rm far}$, $\varepsilon_{\rm split}$, and interior/boundary residual norms.}
  \label{tab:tt-splitting-qc}
\end{table}

Table~\ref{tab:tt-splitting-qc} shows that both routes satisfy $q_{\rm far}(x_s)\approx P_{\rm far}$, with $\varepsilon_{\rm split}$ already at the $10^{-9}$ scale for both rows, while interior Bellman residuals stay at the $10^{-11}$ scale. This closes the mechanism loop behind the fast/slow peak split for both the physics-anchor route and the showcase-quality clear-instance route. We track these residuals together with $\varepsilon_{\rm split}$ as automatic QC scalars in continuous optimization runs.

\subsection*{Committor Implementation in This Report}
\begin{enumerate}
  \item Build the exact transition kernel for each anchor case (including laziness, drift, membrane permeability, and reflecting boundaries).
  \item Use $A=\{x\le x_s+1,\ |y-y_s|\le 1\}$ and $B=\{\text{target}\}$, and solve $q$ via fixed-point iteration with tolerance $10^{-11}$; record interior Bellman residual and boundary mismatch as quality diagnostics, and render the basin/commitment schematic in Figure~\ref{fig:one-target-basin}.
  \item Use exact augmented-state recursion on $(x,\ell,s,r)$ (not MC): $\ell$ tracks leak, $s$ tracks whether $\Sigma_{q^*}$ has been visited, and $r$ tracks strict recross (return to $A$ after visiting $\Sigma_{q^*}$).
  \item Report $q^*=0.5$ as default and additionally provide $q^*\in\{0.4,0.5,0.6\}$ robustness results (Table~\ref{tab:membrane-qstar-sens}).
  \item For no-corridor two-target cases, keep the strict identity $f=f_{\rm near}+f_{\rm far}$ as level-1 decomposition, export the full $q_{\rm far}$ field plus the $q_{\rm far}=0.5$ mask, and then interpret windows with committor-consistent labels.
\end{enumerate}

\section{Takeaways and Next Steps (Committor-Centric)}
\subsection*{Implementation Summary}
\begin{enumerate}
  \item Membrane corridor track: clear-double persists in both symmetric and directional representatives; under strict recross, peak1 is mostly \texttt{L0R0}, valley gains \texttt{L1R0}, and peak2 is mostly \texttt{L0R1}. The new exact occupancy/flux panels further show which regions and crossing directions feed those windows.
  \item No-corridor two-target track: Track B is now explicitly split into a physics anchor $(0.12,2,2)$ and a showcase clear-instance $(0.12,4,1)$. The physics anchor gives $P_{\rm near}/P_{\rm far}\approx0.324/0.676$ with $(t_{p1},t_v,t_{p2})=(7,196,469)$, while the showcase case keeps the strongest visual margins and passes the first robustness gate ($\mathrm{peak\mbox{-}margin}\ge 0.07$, $\mathrm{min\mbox{-}margin}\ge 0.07$, $\mathrm{valley}/\max\le 0.10$). We additionally export strong-gate audit scalars (\texttt{gate-pass}/\texttt{gate-slack}), first-gate candidate count (168), the start-scan continuation CSV, and the $(d,\Delta y)$ loss-mode CSV.
  \item Continuation results now answer the two physics questions directly: in the \texttt{source\_only\_x} start scan only $4/11$ positions remain \texttt{clear}, whereas the comoving control \texttt{source\_plus\_near\_x} stays \texttt{clear} for all $11$ positions; in the $(d,\Delta y)$ near-target scan the dominant failure mode is \texttt{near\_mass\_loss} (22 rows total), with no surviving truncation-risk rows after the adaptive horizon guard.
  \item Methodologically, committor is now fully operational in the pipeline; splitting committor is numerically closed against exact far-hit mass for both the physics anchor and the clear-instance, and we jointly monitor $\varepsilon_{\rm split}$ plus committor residual diagnostics in automatic QC (current $\varepsilon_{\rm split}$ scale $\sim10^{-9}$ for both rows).
  \item Operationally, the current full local regression (codegen + CN/EN build checks) takes about $70\,$s per round (around 1.2 minutes), so it stays in the local-first regime ($<10$ minutes); we switch to Isambard only when projected single-round runtime exceeds $20$ minutes.
\end{enumerate}

\subsection*{Next Directions (All Grounded in Committor)}
\begin{enumerate}
  \item Extend the current physics-anchor continuation from 1D start scans and two $b_x$ slices to the full $(d,\Delta y,b_x)$ continuation surface, while keeping the same loss-mode bookkeeping.
  \item Run $q^*$ sensitivity analysis (e.g., $0.4/0.5/0.6$) and report peak/valley class stability.
  \item Turn the exported splitting-committor fields and window summaries into threshold-margin maps and trajectory-family atlases for the whole two-target scan:
  \[
  q_{\rm far}(x)=\mathbb P_x(\tau_{\rm far}<\tau_{\rm near}),
  \]
  while continuing to use both $\varepsilon_{\rm split}=|q_{\rm far}(x_s)-P_{\rm far}|$ and \texttt{min-margin}/\texttt{gate-slack} as automatic quality checks.
  \item Extend the same committor/flux analysis from directional asymmetry to segmented membranes (for example left/right heterogeneous patches) in a unified geometry view.
  \item Keep continuous build checks active (codegen + EN/CN LaTeX) and enforce routing policy in keepalive: run local for short rounds ($<10$ minutes), and use isbard doctor/auth + submit/status/fetch for large scans (projected round $>20$ minutes), with automatic local-smoke fallback on remote failures.
  \item For every remote round, always record \texttt{JOB\_ID}, \texttt{REMOTE\_DIR}, key artifact paths, and explicit next actions in the run summary.
\end{enumerate}

\section*{Reproducibility}
\begin{verbatim}
cd research/reports/grid2d_membrane_near_target
../../../.venv/bin/python code/membrane_near_target_report.py
cd manuscript
latexmk -pdf -interaction=nonstopmode -halt-on-error \
  -auxdir=build -emulate-aux-dir grid2d_membrane_near_target_en.tex
\end{verbatim}

\end{document}
