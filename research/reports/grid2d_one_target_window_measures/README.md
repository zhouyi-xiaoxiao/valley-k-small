# Grid2D One Target — Window Measures

**Status: WIP scaffold (not yet started).** Directory layout is in place; no code, manuscript, or notes have been written.

## Sub-series

Part of the `grid2d_one_target_*` sub-series. See `grid2d_one_target_base` for shared problem setup, notation, and conventions; this report focuses on window-measure observables specifically.

## Intended scope (working title; refine when work begins)

Window-measure analysis for the single-target 2D-grid FPT distribution — integrated mass, percentile, and tail metrics over fixed time windows, used to characterise FPT shape without committing to a parametric form.

## Layout

- `code/` — empty
- `manuscript/` — empty
- `notes/` — empty
- `artifacts/` — empty placeholder subdirs

## TODO before this report is real

- [ ] Pick the window family (fixed-T, multi-scale, adaptive) and the reported measures
- [ ] Land code + figures
- [ ] Draft `manuscript/grid2d_one_target_window_measures_{en,cn}.tex`
- [ ] Replace this README with a real one matching the style of `cross_luca_regime_map/README.md`
