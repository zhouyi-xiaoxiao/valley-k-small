# ring_lazy_jump_ext

Extension of `ring_lazy_jump` covering β / N sweeps, MC class composition, and tail diagnostics.

## Lineage

- **Predecessor:** `ring_lazy_jump` — base bimodality study; this report extends it with parameter sweeps and MC sampling.
- **Successor:** `ring_lazy_jump_ext_rev2` — revision; supersedes this report. Cite `_ext_rev2` for current results unless specifically referencing the v1 protocol.

## Workflow

See `notes/readme_ext.md` for the full reproduce script (β sweep → choose β → N sweep → MC sweeps → tail diagnostics → optional all-β overlay → build PDFs).

## Manuscript

- `manuscript/ring_lazy_jump_ext_{cn,en}.tex` (+ PDFs)
