% Auto-generated from data/encounter_onset_n_scan.csv; do not edit by hand.
To avoid over-reliance on the single baseline size $N=101$, we additionally scan $N\in\{61,81,101,121,141,161,181,201\}$ with geometry-scaled $(n_0,m_0,\mathrm{src},\mathrm{dst})$.
For each $N$, we recompute has$\geq$2 onset, clear onset, and clear-bimodal coverage on the nominal $\beta\in[0,0.30]$ grid using the same timescale selector as the main scan (first peak + score-selected $t_2$), then compare representative peak/valley ratios at $\beta=0.20$; if no clear onset appears, we extend once to $\beta\le0.50$ (step $0.02$).
Current run summary: 8 size points total, 8 with nominal-window has$\geq$2 onset, 8 with nominal-window clear onset, 0 recovered by extension, 0 still unresolved.
Extension details: No extension-only onset recovery is needed in this run.
Nominal-window conclusion: $N\in\{61,81,101,121,141,161,181,201\}$ all show clear onset inside the nominal window.